# Dependencies & build artifacts (re-created inside the image)
node_modules
.next
out
dist
build

# Generated Prisma client (regenerated in the builder stage)
src/generated

# Secrets & local env — provided at runtime via compose env_file, never baked in
.env
.env.local
.env.*.local

# Docker / infra files (not needed inside the app image)
Dockerfile
.dockerignore
docker-compose*.yml
caddy

# Version control & tooling
.git
.gitignore
.vscode
.idea
skills

# Logs & OS cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Docs
README.md
DEPLOY.md
